---
import BrandMark from "./BrandMark.astro";
import { site } from "../site";

const cols = [
  {
    h: "Product",
    items: [
      { l: "Quickstart",     href: "/quickstart" },
      { l: "Docs",           href: "/docs" },
      { l: "Pricing",        href: "/pricing" },
    ],
  },
  {
    h: "Account",
    items: [
      { l: "Sign in",             href: site.signIn,     external: true },
      { l: "Sign up free",        href: site.signupFree, external: true },
    ],
  },
  {
    h: "Contact",
    items: [
      { l: "Contact us",          href: "/contact" },
      { l: "Talk to us (Pro)",    href: site.proContact, external: false },
      { l: "hello@meshwg.com",    href: "mailto:hello@meshwg.com", external: false },
    ],
  },
  {
    h: "Company",
    items: [
      { l: "Careers",             href: "/careers" },
    ],
  },
];

const year = new Date().getFullYear();
---

<footer>
  <div class="wrap">
    <div class="foot-grid">
      <div class="foot-left">
        <div class="foot-brand">
          <BrandMark />
          <span>{site.productName}</span>
        </div>
        <p class="foot-tag">
          Hosted WireGuard mesh + zero-trust access. Strict per-org isolation, fast-acting policies, works with the routers you already own.
        </p>
      </div>

      <div class="foot-right">
        {cols.map((col) => (
          <div>
            <h5 class="col-h">{col.h}</h5>
            <ul class="col-list">
              {col.items.map((it) => (
                <li>
                  <a
                    href={it.href}
                    target={it.external ? "_blank" : undefined}
                    rel={it.external ? "noopener" : undefined}
                  >
                    {it.l}{it.external ? " ↗" : ""}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </div>

    <div class="foot-bot">
      <div>© {year} {site.productName}. All rights reserved.</div>
      <div class="legal-links">
        <a href="/privacy">Privacy</a>
        <span>·</span>
        <a href="/terms">Terms</a>
        <span>·</span>
        <a href="/refund">Refund &amp; Cancellation</a>
        <span>·</span>
        <a href="/.well-known/security.txt">Security</a>
      </div>
      <div>WireGuard is a registered trademark of Jason A. Donenfeld.</div>
    </div>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid #E2E8F0;
    background: #FAFAFA; /* Solid, clean background color */
    padding: 80px 0 64px;
    color: #475569;
    font-size: 14px;
  }
  .foot-grid {
    display: grid;
    grid-template-columns: 1.2fr 2fr;
    gap: 64px;
  }
  .foot-right {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }
  .foot-brand {
    display: flex; align-items: center; gap: 12px;
    font-weight: 600;
    color: #0F172A;
    font-size: 20px;
    margin-bottom: 16px;
  }
  .foot-tag {
    max-width: 340px;
    margin: 0;
    color: #64748B;
    line-height: 1.6;
    font-size: 14px;
  }

  .col-h {
    font-family: var(--font-mono, monospace);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: .08em;
    color: #64748B;
    font-weight: 600;
    margin: 0 0 16px;
  }
  .col-list {
    list-style: none; padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 12px;
  }
  .col-list a {
    color: #1E293B;
    font-weight: 500;
    text-decoration: none;
    transition: color .12s ease;
  }
  .col-list a:hover { color: #2563EB; }

  .foot-bot {
    margin-top: 64px;
    padding-top: 32px;
    border-top: 1px solid #E2E8F0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 13px;
    color: #64748B;
  }
  .legal-links {
    display: flex; align-items: center; gap: 8px;
  }
  .legal-links a {
    color: #64748B;
    text-decoration: none;
    transition: color .12s ease;
  }
  .legal-links a:hover { color: #2563EB; }
  .legal-links span { color: #94A3B8; }

  @media (max-width: 980px) {
    .foot-grid { grid-template-columns: 1fr; gap: 48px; }
    .foot-right { grid-template-columns: 1fr 1fr; }
    .foot-bot { flex-direction: column; align-items: flex-start; gap: 12px; }
  }
</style>
